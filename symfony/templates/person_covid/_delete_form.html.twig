<form method="post" action="{{ path('person_covid_delete', {'id': person_covid.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ person_covid.id) }}">
    <button class="btn">Delete</button>
</form>

<form method="post" action="{{ path('person_location_delete', {'id': person_location.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ person_location.id) }}">
    <button class="btn">Delete</button>
</form>
